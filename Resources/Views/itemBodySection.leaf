#extend("itemBody"):
    #export("--itemBodyContent"):
    <!-- TODO: Temp solution. Remove later -->
    #if(item.content.body.body.buttonText == "Show works"):
    <a href="#(item.content.body.body.buttonDirection)" class="item-button">
    #else:
    <a href="#(item.content.body.body.buttonDirection)" class="item-button" target="_blank">
    #endif
        #(item.content.body.body.buttonText)
    </a>
    #endexport
#endextend
