<div class="box">
    <div class="content">
        <div>
            <p class="box-title">#(item.title)</p>
            <p class="box-description">#(item.description)</p>
        </div>
        <div>
            #if(item.buttonText):
            #if(item.buttonDirection):
            <a href="#(item.buttonDirection)" class="box-button">#(item.buttonText)</a>
            #endif
            #endif
        </div>
    </div>
</div>
